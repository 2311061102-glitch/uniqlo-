@extends('layouts.admin') @section('title',$category->exists?'Sửa danh mục':'Thêm danh mục') @section('content')<div class="admin-page-header"><h1>{{ $category->exists?'Chỉnh sửa danh mục':'Thêm danh mục mới' }}</h1><a href="{{ route('admin.categories.index') }}" class="link-button">← Quay lại</a></div><form class="admin-form" method="POST" action="{{ $category->exists?route('admin.categories.update',$category):route('admin.categories.store') }}">@csrf @if($category->exists) @method('PUT') @endif<label>Tên danh mục<input name="name" value="{{ old('name',$category->name) }}" required></label><label>Danh mục cha<select name="parent_id"><option value="">Không có</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected(old('parent_id',$category->parent_id)==$parent->id)>{{ $parent->name }}</option>@endforeach</select></label><label>Mô tả<textarea name="description" rows="4">{{ old('description',$category->description) }}</textarea></label><label class="check-row"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->exists?$category->is_active:true))> Hiển thị danh mục</label><button class="admin-button admin-button--dark">Lưu danh mục</button></form>@endsection
