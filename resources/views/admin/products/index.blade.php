@extends('layouts.admin') @section('title','Sản phẩm')
@section('content')
<div class="admin-page-header"><div><p class="admin-eyebrow">CATALOG</p><h1>Quản lý sản phẩm</h1></div><a class="admin-button admin-button--dark" href="{{ route('admin.products.create') }}">+ Thêm sản phẩm</a></div>
<form class="admin-filter" method="GET"><input name="q" value="{{ request('q') }}" placeholder="Tìm theo tên sản phẩm..."><select name="category"><option value="">Tất cả danh mục</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select><select name="status"><option value="">Mọi trạng thái</option><option value="active" @selected(request('status')==='active')>Đang bán</option><option value="inactive" @selected(request('status')==='inactive')>Đang ẩn</option></select><button class="admin-button admin-button--dark">Lọc</button></form>
<div class="admin-panel admin-table-wrap"><table class="admin-table"><thead><tr><th>Sản phẩm</th><th>Danh mục</th><th>Giá</th><th>Tồn kho</th><th>Đánh giá</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($products as $product)<tr><td><strong>{{ $product->name }}</strong><small class="table-muted">{{ $product->material ?: 'Chưa cập nhật chất liệu' }}</small></td><td>{{ $product->category?->name }}</td><td>{{ number_format($product->base_price,0,',','.') }}₫</td><td>{{ $product->total_stock }}</td><td>{{ $product->reviews_count }}</td><td><form method="POST" action="{{ route('admin.products.toggle',$product) }}">@csrf @method('PATCH')<button class="admin-status admin-status--{{ $product->is_active?'completed':'cancelled' }} status-button">{{ $product->is_active?'Đang bán':'Đang ẩn' }}</button></form></td><td class="table-actions"><a href="{{ route('admin.products.edit',$product) }}">Sửa</a><form method="POST" action="{{ route('admin.products.destroy',$product) }}" onsubmit="return confirm('Xóa sản phẩm này?')">@csrf @method('DELETE')<button>Xóa</button></form></td></tr>@empty<tr><td colspan="7" class="admin-empty">Chưa có sản phẩm.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrapper">{{ $products->links() }}</div>
@endsection
