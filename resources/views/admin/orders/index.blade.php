@extends('layouts.admin') @section('title','Đơn hàng') @section('content')<div class="admin-page-header"><div><p class="admin-eyebrow">OPERATIONS</p><h1>Quản lý đơn hàng</h1></div></div><form class="admin-filter" method="GET"><input name="q" value="{{ request('q') }}" placeholder="Mã đơn hoặc tên khách hàng..."><select name="status"><option value="">Tất cả trạng thái</option>@foreach(['pending'=>'Chờ xác nhận','confirmed'=>'Đã xác nhận','processing'=>'Đang xử lý','shipping'=>'Đang giao','completed'=>'Hoàn thành','cancelled'=>'Đã hủy'] as $key=>$label)<option value="{{ $key }}" @selected(request('status')===$key)>{{ $label }}</option>@endforeach</select><button class="admin-button admin-button--dark">Lọc</button></form><div class="admin-panel admin-table-wrap"><table class="admin-table"><thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Ngày đặt</th><th>Thanh toán</th><th>Tổng tiền</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>#{{ $order->order_code }}</strong></td><td>{{ $order->recipient_name }}<small class="table-muted">{{ $order->recipient_phone }}</small></td><td>{{ $order->created_at->format('d/m/Y H:i') }}</td><td>{{ $order->payment_status==='paid'?'Đã thanh toán':'Chưa thanh toán' }}</td><td><strong>{{ number_format($order->total_amount,0,',','.') }}₫</strong></td><td><span class="admin-status admin-status--{{ $order->order_status }}">{{ ['pending'=>'Chờ xác nhận','confirmed'=>'Đã xác nhận','processing'=>'Đang xử lý','shipping'=>'Đang giao','completed'=>'Hoàn thành','cancelled'=>'Đã hủy'][$order->order_status] ?? $order->order_status }}</span></td><td><a href="{{ route('admin.orders.show',$order) }}" class="link-button">Chi tiết</a></td></tr>@empty<tr><td colspan="7" class="admin-empty">Chưa có đơn hàng.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrapper">{{ $orders->links() }}</div>@endsection
